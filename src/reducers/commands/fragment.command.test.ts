import { fragmentCommandsReducer } from "./fragment.command";

describe("fragmentCommandsReducer", () => {
    it("should be defined", () => {
        expect(fragmentCommandsReducer).toBeDefined();
    });
});
